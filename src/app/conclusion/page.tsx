import { Metadata } from "next";
import Conclusion from "./Conclusion";

export const metadata: Metadata = {
  title: "Conclusion | Ives",
};

export default function ConclusionPage() {
  return <Conclusion />;
}
